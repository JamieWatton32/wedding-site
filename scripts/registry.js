/* ============================================================
   REGISTRY.JS — Registry page entrypoint
   ============================================================ */

(function () {
  'use strict';

  if (window.WeddingSite) {
    window.WeddingSite.initShared();
  }
})();
